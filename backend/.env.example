# Backend Environment Variables
# Copy this file to .env and update the values

# MySQL Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=MySQL@24
DB_NAME=portfolio_db

# JWT Configuration (change this to a secure random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Configuration
PORT=5000

# File Upload Path
UPLOAD_PATH=./uploads

# Redis Configuration (Optional - for caching and performance boost)
# If not configured, the app will use in-memory caching automatically

# Option 1: Use Redis URL (recommended for cloud services like Redis Cloud, Upstash)
# REDIS_URL=redis://username:password@host:port
# REDIS_URL=rediss://default:password@redis-host.com:6379  # Use rediss:// for secure TLS connection

# Option 2: Use separate Redis settings (for local Redis or custom setup)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=  # Optional - leave empty if no password

# For local development with Redis installed:
# REDIS_HOST=localhost
# REDIS_PORT=6379

# Note: Redis is completely optional. The app works perfectly without it using in-memory cache.

